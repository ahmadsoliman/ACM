#include <stdio.h>

int arr[105][105];

int main(void){
    int N, i , j, k , l;
    char in[];
    scanf("%d", &N);
    for(i=0; i<N; i++){
        scanf("%s", &in);
        for(j=0; j<N; j++){
            arr[i][j]=;
        }
    }

    return 0;
}
