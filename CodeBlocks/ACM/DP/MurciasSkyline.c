#include <stdio.h>
#include <string.h>

int n, kase, w[1010], h[1010], dp[1010][1010], vis[1010][1010];

int recur(int i, int last){

}

int main(){
    kase=1;
    int t, i,j;
    scanf("%d", &t);
    while(t--){
        scanf("%d", &n);
        h[i]=0;
        for(i=1; i<=n; i++) scanf("%d", &h[i]);
        for(i=1; i<=n; i++) scanf("%d", &w[i]);

    }
    return 0;
}
