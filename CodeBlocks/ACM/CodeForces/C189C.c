#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

char s[105];

int main(){
    int t, n, c, i, j;
    scanf("%d", &t);
    while(t--){
        scanf("%s", s);
        n= strlen(s);

        printf("%d\n", c);
    }
    return 0;
}
