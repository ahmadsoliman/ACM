#include <stdio.h>
#include <string.h>
#include <math.h>
#include <vector>
#include <string>
#include <algorithm>

using namespace std;

typedef long long ll;
typedef pair<int, int> ii;
typedef vector<ii> vii;
typedef vector<int> vi;

#define INF 1000000000

char grid[105][105], in[100];

int main(){
    int n,i,j,k;
    while(scanf("%d", &n)>0){
        for(i=0; i<n; i++) scanf("%s", grid[i]);
        scanf("%s", in);

    }
    return 0;
}
