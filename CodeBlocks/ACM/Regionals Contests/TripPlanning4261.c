#include <stdio.h>
#include <math.h>
#include <string.h>
#include <stdlib.h>

using namespace std;

int n, a[1005];

pair dp[1005][1005];

int recur(){

}

int main(){
    int i,j,k;
    while(scanf("%d", &n)>0){
        if(n==0) return 0;
        for(i=0; i<n; i++) scanf("%d", &a[i]);
    }
    return 0;
}
